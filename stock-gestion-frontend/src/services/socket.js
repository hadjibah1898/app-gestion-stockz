/**
 * Service Socket.io désactivé.
 * Ce fichier est conservé comme placeholder pour éviter les erreurs d'importation.
 */

export const initSocket = (user) => {
    return null;
};

export const getSocket = () => null;

const safeSocket = {
    connect: () => {},
    disconnect: () => {},
    on: () => {},
    off: () => {},
    emit: () => {},
};

export default safeSocket;